<%= form_with(model: family_environment, url:choose_new_or_edit,local: true) do |form| %>
  <% if family_environment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(family_environment.errors.count, "error") %> prohibited this family_environment from being saved:</h2>

      <ul>
        <% family_environment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :relationship %>
    <%= form.text_field :relationship %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.text_field :birthday %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="field">
    <%= form.label :family %>
    <%= form.text_field :family %>
  </div>

  <div class="actions">
    <%= form.submit class: "button" %>
  </div>
<% end %>
